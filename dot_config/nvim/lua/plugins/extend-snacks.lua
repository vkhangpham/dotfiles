return {
  "folke/snacks.nvim",
  keys = {
    -- Remove/disable default explorer keymaps by setting them to false
    { "<leader>e", false }, -- Disable if this was mapped to snacks explorer
    { "<leader>E", false }, -- Disable if this was mapped to snacks explorer
  },
}
